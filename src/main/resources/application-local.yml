spring:
  main:
    banner-mode: "off"
  datasource:
    driverClassName: org.mariadb.jdbc.Driver
    url: "jdbc:mariadb://localhost:3306/aprilskindb"
    username: root
    password: password
  jpa:
    database: mysql
    generate-ddl: false
    hibernate:
      ddl-auto: create
    show-sql: true


